---
phase: 14-integration-pipeline-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/database/migrations/000009_remove_timezone_from_user_plugin_configs.up.sql
  - internal/database/migrations/000009_remove_timezone_from_user_plugin_configs.down.sql
  - internal/plugins/models.go
  - internal/worker/scheduler.go
  - internal/settings/handlers.go
  - internal/settings/viewmodel.go
  - internal/settingsvm/settingsvm.go
  - internal/dashboard/viewmodel.go
  - internal/templates/settings.templ
  - cmd/server/main.go
autonomous: true
requirements:
  - SCHED-04

must_haves:
  truths:
    - "Scheduler falls back to User.Timezone (browser-detected) when evaluating schedules — no per-plugin timezone"
    - "UserPluginConfig.Timezone column is dropped from database"
    - "Settings UI no longer shows per-plugin timezone picker"
    - "User-level account settings page exists with timezone picker"
    - "Dashboard tiles and settings page compute next-run using User.Timezone via users table JOIN"
  artifacts:
    - path: "internal/database/migrations/000009_remove_timezone_from_user_plugin_configs.up.sql"
      provides: "Migration to drop timezone column"
      contains: "DROP COLUMN"
    - path: "internal/worker/scheduler.go"
      provides: "Scheduler using cfg.User.Timezone instead of cfg.Timezone"
      contains: "cfg.User.Timezone"
    - path: "internal/templates/settings.templ"
      provides: "Account settings page template with timezone picker"
      contains: "AccountSettingsPage"
    - path: "internal/settings/handlers.go"
      provides: "AccountSettingsPageHandler and SaveTimezoneHandler"
      contains: "AccountSettingsPageHandler"
  key_links:
    - from: "internal/worker/scheduler.go"
      to: "User.Timezone"
      via: "cfg.User.Timezone with Preload('User') already in place"
      pattern: "cfg\\.User\\.Timezone"
    - from: "internal/dashboard/viewmodel.go"
      to: "users table"
      via: "JOIN users u ON u.id = upc.user_id for timezone in configRow"
      pattern: "JOIN users"
    - from: "cmd/server/main.go"
      to: "internal/settings/handlers.go"
      via: "Routes /settings/account and /api/user/settings/timezone"
      pattern: "settings/account"
---

<objective>
Remove per-plugin timezone from UserPluginConfig and wire all timezone references to User.Timezone. Add user-level account settings page with timezone picker. Clean up stale scheduler comment and all timezone references in settings/dashboard code.

Purpose: SCHED-04 requires timezone-aware schedule matching using user's local time. The per-plugin timezone was never properly connected — the scheduler fell back to UTC. User decision simplifies this to a single user-level timezone.

Output: Migration dropping timezone column, scheduler using User.Timezone, account settings page at /settings/account with timezone picker, all SQL queries joining users table for timezone.
</objective>

<execution_context>
@/Users/jim/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-integration-pipeline-fix/14-RESEARCH.md
@internal/plugins/models.go
@internal/worker/scheduler.go
@internal/settings/handlers.go
@internal/settings/viewmodel.go
@internal/settingsvm/settingsvm.go
@internal/dashboard/viewmodel.go
@internal/templates/settings.templ
@cmd/server/main.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration, model update, and scheduler timezone fix</name>
  <files>
    internal/database/migrations/000009_remove_timezone_from_user_plugin_configs.up.sql
    internal/database/migrations/000009_remove_timezone_from_user_plugin_configs.down.sql
    internal/plugins/models.go
    internal/worker/scheduler.go
  </files>
  <action>
**Migration 000009 (up):**
```sql
ALTER TABLE user_plugin_configs DROP COLUMN IF EXISTS timezone;
```

**Migration 000009 (down):**
```sql
ALTER TABLE user_plugin_configs ADD COLUMN IF NOT EXISTS timezone VARCHAR(100) NOT NULL DEFAULT 'UTC';
```

**Model (internal/plugins/models.go):**
Remove the `Timezone string` field from the `UserPluginConfig` struct. Leave all other fields unchanged.

**Scheduler (internal/worker/scheduler.go):**

Find the `handlePerMinuteScheduler` function. It already does `Preload("User")` when querying UserPluginConfigs, so `cfg.User` is populated.

Replace the timezone resolution block (approximately lines 91-95) that reads `cfg.Timezone` with:
```go
effectiveTimezone := cfg.User.Timezone
if effectiveTimezone == "" {
    effectiveTimezone = "UTC"
}
```

Remove the stale comment that says "User model does not yet have a Timezone field" — the User model has had `Timezone string` since migration 000001.

Sweep `scheduler.go` for any other references to `cfg.Timezone` and update to `cfg.User.Timezone`.
  </action>
  <verify>
Run `go build ./...` to confirm compilation. Verify `Timezone` is NOT in `UserPluginConfig` struct. Verify `cfg.User.Timezone` is used in scheduler. Verify migration files exist.
  </verify>
  <done>
Database migration drops timezone column from user_plugin_configs. Model struct has no Timezone field. Scheduler reads User.Timezone with UTC fallback. Stale comment removed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove per-plugin timezone from settings/dashboard and add account settings page</name>
  <files>
    internal/settings/handlers.go
    internal/settings/viewmodel.go
    internal/settingsvm/settingsvm.go
    internal/dashboard/viewmodel.go
    internal/templates/settings.templ
    cmd/server/main.go
  </files>
  <action>
**Settings handlers (internal/settings/handlers.go):**

1. In `SaveSettingsHandler`: Remove the lines that read `timezone := rawForm.Get("timezone")` and set `config.Timezone = timezone`. There should be two locations per research.

2. In `TogglePluginHandler`: Remove `Timezone: "UTC"` from the `UserPluginConfig{}` struct literal used when creating new records.

3. Add `AccountSettingsPageHandler(db *gorm.DB) gin.HandlerFunc` — GET handler that:
   - Gets the authenticated user from session
   - Loads user record from DB (to get current timezone)
   - Renders `AccountSettingsPage(user.Name, user.Timezone)` template

4. Add `SaveTimezoneHandler(db *gorm.DB) gin.HandlerFunc` — POST handler that:
   - Gets authenticated user from session
   - Reads `timezone` from form
   - Validates timezone with `time.LoadLocation(timezone)` — return 400 on invalid
   - Updates `user.Timezone = timezone` in DB via `db.Model(&user).Update("timezone", timezone)`
   - Returns success HTML fragment (e.g., a toast or updated timezone display)

**Settings viewmodel (internal/settings/viewmodel.go):**

1. In the SQL query inside `buildPluginSettingsViewModels` (or equivalent): Remove `COALESCE(upc.timezone, 'UTC') AS timezone` from the SELECT. If timezone is used by `computeNextRun` in `getPluginStatus`, add `JOIN users u ON u.id = upc.user_id` and select `u.timezone AS timezone` instead (sourcing from users table).

2. Remove `Timezone string` from the `pluginRow` scan struct if it's no longer needed — OR replace its source with `u.timezone`.

3. In `BuildSinglePluginSettingsViewModel` or similar: Remove any Timezone field assignment to the viewmodel.

**Settings viewmodel types (internal/settingsvm/settingsvm.go):**

Remove `Timezone string` field from `PluginSettingsViewModel` struct.

**Dashboard viewmodel (internal/dashboard/viewmodel.go):**

NOTE: This file is also modified by Plan 01 (OutputSection struct, extractContent). The changes here are to DIFFERENT parts of the file — the SQL queries and configRow struct. No conflict.

1. In `getDashboardTiles` SQL query: Replace `upc.timezone` with `u.timezone AS timezone`. Add `JOIN users u ON u.id = upc.user_id` to the query.

2. In `GetSingleTile` SQL query: Same change — replace `upc.timezone` with `u.timezone` via users JOIN.

3. The `configRow` struct field `Timezone string` can keep its name — just source it from the users table now.

**Settings template (internal/templates/settings.templ):**

1. Remove the `renderTimezoneSelect` call from within `PluginSettingsForm` (the per-plugin timezone picker). The `renderTimezoneSelect` component itself should be KEPT since it will be reused on the account settings page.

2. Add a new `AccountSettingsPage` templ component:
   - Wrapped in `@Layout("Account Settings - First Sip")` with `@AppNavbar("settings")`
   - Shows a `.glass-card` with "Account Settings" heading
   - Contains the timezone picker using the existing `renderTimezoneSelect` component (or similar select with IANA timezone list)
   - Form posts to `/api/user/settings/timezone` via hx-post
   - Shows current timezone value as selected option
   - On success, show brief confirmation (hx-swap appropriate target)

3. Update sidebar sub-links: In the settings sidebar (where "Plugins" sub-link exists), add an "Account" sub-link pointing to `/settings/account`. Update the active state logic to handle both `activePage == "plugins"` and `activePage == "account"`.

**Routes (cmd/server/main.go):**

Add two routes inside the authenticated group:
```go
r.GET("/settings/account", settings.AccountSettingsPageHandler(db))
r.POST("/api/user/settings/timezone", settings.SaveTimezoneHandler(db))
```

**Run templ-generate** after template changes:
```bash
make templ-generate
```

**Dead code sweep:** Check all modified files for any remaining references to per-plugin timezone (`config.Timezone`, `upc.timezone`, `cfg.Timezone`) and remove them.
  </action>
  <verify>
Run `make templ-generate && go build ./...` to confirm everything compiles. Grep for `config.Timezone` and `upc.timezone` across codebase — should return zero hits. Verify `/settings/account` route exists in main.go. Verify `AccountSettingsPage` component exists in settings.templ.
  </verify>
  <done>
Per-plugin timezone removed from all settings handlers, viewmodels, and SQL queries. Dashboard and settings queries JOIN users table for timezone. Account settings page renders at /settings/account with timezone picker. Sidebar shows Account sub-link. All per-plugin timezone references eliminated.
  </done>
</task>

</tasks>

<verification>
1. `make templ-generate && go build ./...` — full build succeeds
2. `grep -rn "config\.Timezone" internal/` — zero results (per-plugin timezone gone)
3. `grep -rn "upc\.timezone" internal/` — zero results (SQL references gone)
4. `grep -rn "cfg\.Timezone" internal/` — zero results (scheduler reference gone)
5. `grep -n "cfg.User.Timezone" internal/worker/scheduler.go` — present (correct fallback)
6. `grep -n "JOIN users" internal/dashboard/viewmodel.go` — present (timezone from users table)
7. `grep -n "AccountSettingsPage" internal/templates/settings.templ` — present
8. `grep -n "settings/account" cmd/server/main.go` — route registered
9. `ls internal/database/migrations/000009*` — both up and down migrations exist
10. `grep -n "Timezone" internal/plugins/models.go` — NOT present in UserPluginConfig struct
</verification>

<success_criteria>
- Migration 000009 drops timezone column from user_plugin_configs
- UserPluginConfig model has no Timezone field
- Scheduler uses cfg.User.Timezone with UTC fallback
- Stale scheduler comment removed
- SaveSettingsHandler and TogglePluginHandler have no per-plugin timezone code
- Dashboard SQL queries JOIN users table for timezone
- Settings SQL queries JOIN users table for timezone
- Per-plugin timezone picker removed from settings form
- Account settings page exists at /settings/account with timezone picker
- Sidebar has Account sub-link under Settings
- Routes registered in main.go
- Zero references to per-plugin timezone remain in codebase
</success_criteria>

<output>
After completion, create `.planning/phases/14-integration-pipeline-fix/14-02-SUMMARY.md`
</output>
