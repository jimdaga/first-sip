---
phase: 15-verification-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/10-per-user-scheduling/10-VERIFICATION.md
  - .planning/phases/11-tile-based-dashboard/11-VERIFICATION.md
autonomous: true
requirements:
  - SCHED-01
  - SCHED-02
  - SCHED-03
  - SCHED-05
  - SCHED-06
  - TILE-01
  - TILE-02
  - TILE-03
  - TILE-04
  - TILE-05
  - TILE-06

must_haves:
  truths:
    - "Phase 10 has a VERIFICATION.md with SCHED-01/02/03/05/06 verified against actual source files with line numbers"
    - "Phase 11 has a VERIFICATION.md with TILE-01/02/03/04/05/06 verified against actual source files with line numbers"
    - "Neither VERIFICATION.md references removed/stale code (e.g., UserPluginConfig.Timezone)"
    - "Phase 10 VERIFICATION.md does NOT include SCHED-04 (that belongs to Phase 14)"
  artifacts:
    - path: ".planning/phases/10-per-user-scheduling/10-VERIFICATION.md"
      provides: "Static code verification for SCHED-01/02/03/05/06"
      contains: "status: passed"
    - path: ".planning/phases/11-tile-based-dashboard/11-VERIFICATION.md"
      provides: "Static code verification for TILE-01/02/03/04/05/06"
      contains: "status: passed"
  key_links:
    - from: "10-VERIFICATION.md"
      to: "internal/worker/scheduler.go"
      via: "file:line citations for SCHED requirements"
      pattern: "scheduler\\.go"
    - from: "11-VERIFICATION.md"
      to: "internal/templates/dashboard.templ"
      via: "file:line citations for TILE requirements"
      pattern: "dashboard\\.templ"
---

<objective>
Write VERIFICATION.md files for Phase 10 (Per-User Scheduling) and Phase 11 (Tile-Based Dashboard) to close 11 orphaned verification gaps from the v1.1 milestone audit.

Purpose: Phases 10 and 11 completed their code plans but never received formal verification documents, leaving 11 requirement IDs unverified. This plan produces the missing verification reports.
Output: Two VERIFICATION.md files following the project's established Observable Truths pattern with file:line evidence.
</objective>

<execution_context>
@/Users/jim/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-verification-closure/15-RESEARCH.md
@.planning/phases/13-account-tier-scaffolding/13-VERIFICATION.md
@.planning/phases/14-integration-pipeline-fix/14-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Phase 10 VERIFICATION.md for SCHED-01/02/03/05/06</name>
  <files>.planning/phases/10-per-user-scheduling/10-VERIFICATION.md</files>
  <action>
Create `.planning/phases/10-per-user-scheduling/10-VERIFICATION.md` following the exact format used in `13-VERIFICATION.md` (YAML frontmatter + Observable Truths table + Required Artifacts + Key Links + Requirements Coverage + Human Verification).

**CRITICAL PITFALL AVOIDANCE:**
- Do NOT include SCHED-04 — it was verified in Phase 14's VERIFICATION.md, not Phase 10.
- Do NOT reference `UserPluginConfig.Timezone` as a current field — it was dropped by Phase 14 migration 000009. For SCHED-01, note that timezone is now sourced from `users.timezone` via JOIN.
- Do NOT cite SUMMARY.md or 10-UAT.md as primary evidence. Every truth claim must cite actual source files with line numbers from codebase inspection.

**Requirements to verify (5 total):**

SCHED-01 (per-user per-plugin cron config): Inspect `internal/plugins/models.go` for `CronExpression` field on `UserPluginConfig`; inspect `internal/database/migrations/000006_add_schedule_to_user_plugin_configs.up.sql` for column addition; inspect `internal/settings/handlers.go` for `SaveSettingsHandler` persisting cron expression. Note timezone now on `users` table (Phase 14 migration).

SCHED-02 (database-backed evaluation): Inspect `internal/worker/scheduler.go` for `handlePerMinuteScheduler` DB query (`WHERE enabled = ? AND cron_expression IS NOT NULL`). Grep codebase to confirm zero per-user Asynq cron registrations.

SCHED-03 (per-minute scheduler task): Inspect `internal/worker/scheduler.go` for `scheduler.Register("* * * * *", ...)`. Inspect `internal/worker/tasks.go` for `TaskPerMinuteScheduler` constant. Inspect `cmd/server/main.go` for `StartPerMinuteScheduler` call.

SCHED-05 (global cron removed): Grep entire codebase for `StartScheduler` (bare function name), `TaskScheduledBriefingGeneration`, `handleScheduledBriefingGeneration`, `BriefingSchedule`, `BriefingTimezone` — expect zero matches for each. This confirms complete removal.

SCHED-06 (Redis last-run cache): Inspect `internal/worker/scheduler.go` for `lastRunHashKey`, `HGet`, `HSet` calls. Inspect `isDue()` for cold-cache protection (zero lastRunAt treated as one minute ago).

**Frontmatter:** Use `phase: 10-per-user-scheduling`, `status: passed`, `score: N/N must-haves verified` (count the observable truths you write). Include at least one `human_verification` item for live scheduler timing verification.

**Observable Truths:** Write 5-7 truths, one per requirement minimum, with VERIFIED status and file:line evidence.

**Required Artifacts:** List the key source files (scheduler.go, models.go, tasks.go, migration 000006).

**Key Links:** Wire scheduler.go -> tasks.go -> main.go -> models.go connections.

**Requirements Coverage table:** List SCHED-01/02/03/05/06 only. Mark all as VERIFIED.

**UAT.md note:** In the Summary section, mention that `10-UAT.md` provides corroborating manual test evidence for 8/8 tests passed, but this VERIFICATION.md is the authoritative static code verification.
  </action>
  <verify>
1. File exists at `.planning/phases/10-per-user-scheduling/10-VERIFICATION.md`
2. YAML frontmatter contains `status: passed` and valid `score` field
3. Observable Truths table has at least 5 rows, all with VERIFIED status
4. Requirements Coverage table lists exactly SCHED-01, SCHED-02, SCHED-03, SCHED-05, SCHED-06 (NOT SCHED-04)
5. No reference to `UserPluginConfig.Timezone` as a current field
6. `go build ./...` still passes (no code changes, but confirm build green)
  </verify>
  <done>Phase 10 VERIFICATION.md exists with all 5 scheduling requirements (SCHED-01/02/03/05/06) verified against actual source files with line number citations. SCHED-04 is correctly excluded. No stale references to removed columns.</done>
</task>

<task type="auto">
  <name>Task 2: Write Phase 11 VERIFICATION.md for TILE-01/02/03/04/05/06</name>
  <files>.planning/phases/11-tile-based-dashboard/11-VERIFICATION.md</files>
  <action>
Create `.planning/phases/11-tile-based-dashboard/11-VERIFICATION.md` following the exact format used in `13-VERIFICATION.md`.

**Requirements to verify (6 total):**

TILE-01 (CSS Grid tile layout): Inspect `static/css/liquid-glass.css` for `.tile-grid` class with `grid-template-columns: repeat(auto-fit, minmax(280px, 1fr))`. Inspect `internal/templates/dashboard.templ` for `TileGrid` component using `tile-grid` class. Note responsive breakpoint at 640px.

TILE-02 (plugin name + briefing summary + status per tile): Inspect `dashboard.templ` for `TileCard` component rendering `DisplayName`, `BriefingSummary`, and status icon (spinner for pending/processing, error SVG for HasError). Inspect `internal/tiles/viewmodel.go` for `TileViewModel` struct carrying all three data points.

TILE-03 (last run time + next scheduled run): Inspect `internal/dashboard/viewmodel.go` for `formatTimingTooltip()` building "Last run: X - Next: Y" string. Inspect `dashboard.templ` for `data-tooltip` attribute on `tile-info-badge`. Note `computeNextRun()` uses user's IANA timezone from users table JOIN.

TILE-04 (pre-fetch without N+1): Inspect `internal/dashboard/viewmodel.go` `getDashboardTiles()` for exactly 3 queries. Verify `DISTINCT ON (plugin_id)` usage for latest run and latest successful run. Verify map-based O(1) assembly (no per-tile DB calls).

TILE-05 (empty states): Inspect `dashboard.templ` for `TileOnboarding` component (no-plugins-enabled state with link to settings). Inspect collapsed/expanded content areas for "first briefing is scheduled for..." waiting state when no BriefingSummary exists.

TILE-06 (HTMX in-place updates): Inspect `dashboard.templ` for conditional `hx-get`, `hx-trigger="every 30s"`, `hx-swap="outerHTML settle:0.6s"` attributes — only added when tile status is pending/processing. Inspect `internal/dashboard/handlers.go` for `TileStatusHandler` rendering `TileCard`.

**Frontmatter:** Use `phase: 11-tile-based-dashboard`, `status: passed`, `score: N/N must-haves verified`. Include at least one `human_verification` item for drag-and-drop tile ordering persistence.

**Observable Truths:** Write 6-8 truths, one per requirement minimum, with VERIFIED status and file:line evidence.

**Required Artifacts:** List dashboard.templ, viewmodel.go, handlers.go, liquid-glass.css, tiles/viewmodel.go.

**Key Links:** Wire dashboard.templ -> viewmodel.go -> handlers.go -> models connections.

**Requirements Coverage table:** List TILE-01 through TILE-06. Mark all as VERIFIED.
  </action>
  <verify>
1. File exists at `.planning/phases/11-tile-based-dashboard/11-VERIFICATION.md`
2. YAML frontmatter contains `status: passed` and valid `score` field
3. Observable Truths table has at least 6 rows, all with VERIFIED status
4. Requirements Coverage table lists exactly TILE-01 through TILE-06
5. Evidence cites actual file paths and line numbers (not SUMMARY.md references)
6. `go build ./...` still passes
  </verify>
  <done>Phase 11 VERIFICATION.md exists with all 6 tile requirements (TILE-01/02/03/04/05/06) verified against actual source files with line number citations. Empty states, HTMX polling, and N+1 avoidance all documented with evidence.</done>
</task>

</tasks>

<verification>
1. Both VERIFICATION.md files exist in their respective phase directories
2. Both use the project's established YAML frontmatter + Observable Truths format (matching Phase 13 template)
3. All 11 requirements across both files have VERIFIED status with file:line evidence
4. Phase 10 file does NOT include SCHED-04 (correctly excluded)
5. Phase 10 file does NOT reference UserPluginConfig.Timezone as current (correctly notes Phase 14 migration)
6. Build remains green (`go build ./...` passes)
</verification>

<success_criteria>
- `.planning/phases/10-per-user-scheduling/10-VERIFICATION.md` exists with 5 SCHED requirements verified
- `.planning/phases/11-tile-based-dashboard/11-VERIFICATION.md` exists with 6 TILE requirements verified
- All evidence uses file:line citations from actual codebase inspection
- No stale references to removed code or wrong phase assignments
</success_criteria>

<output>
After completion, create `.planning/phases/15-verification-closure/15-01-SUMMARY.md`
</output>
