---
phase: 16-api-key-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/models/user_api_key.go
  - internal/models/user.go
  - internal/database/migrations/000010_create_user_api_keys.up.sql
  - internal/database/migrations/000010_create_user_api_keys.down.sql
  - internal/database/migrations/000011_add_llm_preferences_to_users.up.sql
  - internal/database/migrations/000011_add_llm_preferences_to_users.down.sql
  - internal/apikeys/service.go
  - internal/apikeys/providers.go
autonomous: true
requirements:
  - KEYS-01
  - KEYS-02
  - KEYS-04
  - KEYS-05

must_haves:
  truths:
    - "LLM API keys are encrypted at rest using AES-256-GCM via GORM BeforeSave/AfterFind hooks"
    - "Tavily search API keys are encrypted at rest using the same mechanism"
    - "A user can have one key per provider (enforced by partial unique index)"
    - "Preferred LLM provider and model are stored as columns on the users table"
    - "Service layer provides CRUD operations for API keys and LLM preference"
  artifacts:
    - path: "internal/models/user_api_key.go"
      provides: "UserAPIKey model with BeforeSave/AfterFind encryption hooks"
      contains: "BeforeSave"
    - path: "internal/database/migrations/000010_create_user_api_keys.up.sql"
      provides: "user_api_keys table with partial unique index"
      contains: "CREATE TABLE user_api_keys"
    - path: "internal/database/migrations/000011_add_llm_preferences_to_users.up.sql"
      provides: "llm_preferred_provider and llm_preferred_model columns on users"
      contains: "ALTER TABLE users"
    - path: "internal/apikeys/service.go"
      provides: "CRUD functions for API key management"
      contains: "func SaveKey"
    - path: "internal/apikeys/providers.go"
      provides: "Curated list of supported LLM providers and models"
      contains: "SupportedLLMProviders"
  key_links:
    - from: "internal/models/user_api_key.go"
      to: "internal/models/auth_identity.go"
      via: "shared package-level encryptor variable"
      pattern: "encryptor\\.Encrypt"
    - from: "internal/apikeys/service.go"
      to: "internal/models/user_api_key.go"
      via: "GORM Create/Save/Delete operations"
      pattern: "db\\.(Create|Save|Delete)"
---

<objective>
Create the data layer for API key management: the UserAPIKey GORM model with transparent encryption hooks, SQL migrations for the user_api_keys table and LLM preference columns on users, a curated provider/model list, and a service layer providing CRUD operations.

Purpose: Establishes the encrypted storage foundation that Plan 02's UI layer will consume. All API keys are encrypted at rest using the same AES-256-GCM pattern proven by AuthIdentity.
Output: UserAPIKey model, two SQL migrations, service package with CRUD functions, provider/model definitions.
</objective>

<execution_context>
@/Users/jim/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-api-key-management/16-RESEARCH.md
@internal/models/auth_identity.go
@internal/models/user.go
@internal/database/migrations/000001_create_users.up.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UserAPIKey model, SQL migrations, and LLM preference columns</name>
  <files>
    internal/models/user_api_key.go
    internal/models/user.go
    internal/database/migrations/000010_create_user_api_keys.up.sql
    internal/database/migrations/000010_create_user_api_keys.down.sql
    internal/database/migrations/000011_add_llm_preferences_to_users.up.sql
    internal/database/migrations/000011_add_llm_preferences_to_users.down.sql
  </files>
  <action>
    1. Create `internal/models/user_api_key.go` in the `models` package:
       - Define `UserAPIKey` struct with fields: `gorm.Model`, `UserID uint`, `User User` (cascade delete FK), `KeyType string` ("llm" or "tavily"), `Provider string` (default empty string — "openai", "anthropic", "groq", or "" for tavily), `EncryptedValue string` (type:text).
       - Implement `BeforeSave(tx *gorm.DB) error` — encrypts `EncryptedValue` using the package-level `encryptor` variable (already declared in `auth_identity.go`). Guard with `if encryptor == nil { return nil }` for test compatibility.
       - Implement `AfterFind(tx *gorm.DB) error` — decrypts `EncryptedValue` using the same `encryptor`. Same nil guard.
       - Follow the exact pattern from `auth_identity.go` — do NOT create a new encryptor variable. The existing one in the `models` package is shared.

    2. Add LLM preference fields to `User` struct in `internal/models/user.go`:
       - Add `LLMPreferredProvider string` with GORM tag `gorm:"not null;default:''"`.
       - Add `LLMPreferredModel string` with GORM tag `gorm:"not null;default:''"`.
       - Add `APIKeys []UserAPIKey` association with `gorm:"constraint:OnDelete:CASCADE;"`.

    3. Create migration `000010_create_user_api_keys.up.sql`:
       ```sql
       CREATE TABLE user_api_keys (
           id BIGSERIAL PRIMARY KEY,
           created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
           updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
           deleted_at TIMESTAMPTZ,
           user_id BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
           key_type VARCHAR(50) NOT NULL,
           provider VARCHAR(50) NOT NULL DEFAULT '',
           encrypted_value TEXT NOT NULL
       );
       CREATE INDEX idx_user_api_keys_deleted_at ON user_api_keys(deleted_at);
       CREATE INDEX idx_user_api_keys_user_id ON user_api_keys(user_id);
       CREATE UNIQUE INDEX idx_user_api_keys_unique_active ON user_api_keys(user_id, key_type, provider) WHERE deleted_at IS NULL;
       ```
       Use a PARTIAL unique index (`WHERE deleted_at IS NULL`) to allow re-adding keys after soft delete — same pattern as `idx_users_email_not_deleted` in migration 000001.

    4. Create migration `000010_create_user_api_keys.down.sql`:
       ```sql
       DROP TABLE IF EXISTS user_api_keys;
       ```

    5. Create migration `000011_add_llm_preferences_to_users.up.sql`:
       ```sql
       ALTER TABLE users
           ADD COLUMN IF NOT EXISTS llm_preferred_provider VARCHAR(50) NOT NULL DEFAULT '',
           ADD COLUMN IF NOT EXISTS llm_preferred_model VARCHAR(100) NOT NULL DEFAULT '';
       ```

    6. Create migration `000011_add_llm_preferences_to_users.down.sql`:
       ```sql
       ALTER TABLE users
           DROP COLUMN IF EXISTS llm_preferred_provider,
           DROP COLUMN IF EXISTS llm_preferred_model;
       ```
  </action>
  <verify>
    Run `make build` — compiles without errors (confirms model and struct changes are valid Go).
    Run `make db-up && make dev` briefly to confirm migrations apply without errors (check server log for "Failed to run migrations").
  </verify>
  <done>
    UserAPIKey model exists in models package with BeforeSave/AfterFind encryption hooks sharing the existing encryptor. Two SQL migrations exist: 000010 creates user_api_keys table with partial unique index, 000011 adds LLM preference columns to users. User struct has LLMPreferredProvider, LLMPreferredModel fields and APIKeys association.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create API key service layer and provider definitions</name>
  <files>
    internal/apikeys/service.go
    internal/apikeys/providers.go
  </files>
  <action>
    1. Create `internal/apikeys/providers.go`:
       - Define `LLMProvider` struct: `ID string`, `Name string`, `Models []string`.
       - Define `var SupportedLLMProviders = []LLMProvider{...}` with:
         - `{ID: "openai", Name: "OpenAI", Models: []string{"gpt-4o", "gpt-4o-mini", "gpt-4-turbo", "gpt-3.5-turbo"}}`
         - `{ID: "anthropic", Name: "Anthropic", Models: []string{"claude-opus-4-5", "claude-sonnet-4-5", "claude-haiku-3-5"}}`
         - `{ID: "groq", Name: "Groq", Models: []string{"llama-3.3-70b-versatile", "llama-3.1-8b-instant", "mixtral-8x7b-32768"}}`
       - Add helper `func GetProviderByID(id string) *LLMProvider` that returns a pointer to the matching provider or nil.
       - Add helper `func GetAllModelsForProvider(providerID string) []string` that returns the model list for a provider.

    2. Create `internal/apikeys/service.go`:
       - Package `apikeys`.
       - Import `gorm.io/gorm` and `github.com/jimdaga/first-sip/internal/models`.
       - Define `func SaveKey(db *gorm.DB, userID uint, keyType, provider, value string) error`:
         - Try to find existing key: `db.Where("user_id = ? AND key_type = ? AND provider = ?", userID, keyType, provider).First(&existing)`.
         - If found: update `existing.EncryptedValue = value`, then `db.Save(&existing)`.
         - If not found: create new `models.UserAPIKey{UserID: userID, KeyType: keyType, Provider: provider, EncryptedValue: value}` via `db.Create`.
         - This handles both add and update in one function (upsert pattern).
       - Define `func DeleteKey(db *gorm.DB, userID uint, keyID uint) error`:
         - Soft delete: `db.Where("id = ? AND user_id = ?", keyID, userID).Delete(&models.UserAPIKey{})`.
         - The `user_id` check prevents deleting another user's key.
       - Define `func GetKeysForUser(db *gorm.DB, userID uint) ([]models.UserAPIKey, error)`:
         - `db.Where("user_id = ?", userID).Find(&keys)` — GORM AfterFind hook auto-decrypts.
       - Define `func GetKeyByID(db *gorm.DB, userID uint, keyID uint) (*models.UserAPIKey, error)`:
         - `db.Where("id = ? AND user_id = ?", keyID, userID).First(&key)`.
       - Define `func SaveLLMPreference(db *gorm.DB, userID uint, provider, model string) error`:
         - Validate provider is in `SupportedLLMProviders` list. Return error if not.
         - Validate model is in the provider's Models list. Return error if not.
         - `db.Model(&models.User{}).Where("id = ?", userID).Updates(map[string]interface{}{"llm_preferred_provider": provider, "llm_preferred_model": model})`.
       - Define `func MaskAPIKey(plaintext string) string`:
         - If `len(plaintext) <= 7`: return `"***"`.
         - Otherwise: return `plaintext[:3] + "..." + plaintext[len(plaintext)-4:]`.
         - This is called by the UI layer (Plan 02) at view model build time.
  </action>
  <verify>
    Run `make build` — compiles without errors.
    Verify `internal/apikeys/service.go` and `internal/apikeys/providers.go` exist and compile.
  </verify>
  <done>
    Service layer provides SaveKey (upsert), DeleteKey (soft delete with user ownership check), GetKeysForUser, GetKeyByID, SaveLLMPreference (with provider/model validation), and MaskAPIKey. Provider definitions list OpenAI, Anthropic, and Groq with their current models. All functions operate on the UserAPIKey model whose GORM hooks handle encryption transparently.
  </done>
</task>

</tasks>

<verification>
1. `make build` passes — all new Go files compile.
2. `internal/models/user_api_key.go` uses the shared `encryptor` variable from `auth_identity.go` (no new encryptor declaration).
3. Migration 000010 creates `user_api_keys` table with partial unique index (`WHERE deleted_at IS NULL`).
4. Migration 000011 adds `llm_preferred_provider` and `llm_preferred_model` to `users` table.
5. `internal/apikeys/service.go` exports SaveKey, DeleteKey, GetKeysForUser, GetKeyByID, SaveLLMPreference, MaskAPIKey.
6. `internal/apikeys/providers.go` exports SupportedLLMProviders with 3 providers.
</verification>

<success_criteria>
- UserAPIKey model with working encryption hooks shares the existing models.encryptor
- SQL migrations apply cleanly on `make dev` startup
- Service layer compiles and provides all CRUD + mask + preference operations
- Provider list defines OpenAI, Anthropic, Groq with their models
</success_criteria>

<output>
After completion, create `.planning/phases/16-api-key-management/16-01-SUMMARY.md`
</output>
