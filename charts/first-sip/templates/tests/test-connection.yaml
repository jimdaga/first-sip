apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "first-sip.fullname" . }}-test-connection"
  labels:
    {{- include "first-sip.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "first-sip.fullname" . }}:{{ .Values.service.port }}/health']
  restartPolicy: Never
