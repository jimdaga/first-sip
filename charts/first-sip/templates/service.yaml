apiVersion: v1
kind: Service
metadata:
  name: {{ include "first-sip.fullname" . }}
  labels:
    {{- include "first-sip.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "first-sip.selectorLabels" . | nindent 4 }}
